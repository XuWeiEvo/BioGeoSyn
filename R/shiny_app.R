#' Launch the future iBiogeobears Shiny application
#'
#' @return Invisibly returns `NULL`.
#' @export
launch_app <- function() {
  stop(
    "The Shiny GUI is planned for version 0.3. ",
    "Use run_workflow() for the reproducible backend workflow first.",
    call. = FALSE
  )
}
